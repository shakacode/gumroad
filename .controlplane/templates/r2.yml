kind: policy
name: "{{APP_NAME}}-r2-access"
bindings:
  - permissions:
      - reveal
    principalLinks:
      - "{{APP_IDENTITY_LINK}}"
targetKind: secret
targetLinks:
  - "//secret/{{APP_NAME}}-r2"
