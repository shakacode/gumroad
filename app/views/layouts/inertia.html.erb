<!DOCTYPE html>
<html lang="en">
  <%= render("layouts/application/head") %>
  <% bclass = ["group/body"] %>
  <% bclass.push "mac" if request.headers["User-Agent"]&.include? "Macintosh" %>
  <% bclass.push(@body_class) if @body_class %>
  <% custom_context = @precomputed_rendering_context || RenderingExtension.custom_context(self) %>
  <body id="<%= @body_id %>" class="<%= bclass.join(" ") %>" style="<%= params[:as_embed] && "background: transparent" %>">
    <div id="design-settings" data-settings="<%= custom_context[:design_settings].to_json %>" style="display: none;"></div>
    <div id="user-agent-info" data-settings="<%= custom_context[:user_agent_info].to_json %>" style="display: none;"></div>
    <%= render("layouts/shared/flash") %>
    <%= render("discover/seo_links") if @discover_category_links.present? %>
    <%= content_for?(:content) ? yield(:content) : yield %>
    <% if content_for? :footer %>
      <footer>
        <div style="text-align: center; padding: 1rem 0;">
          <%= yield :footer %>
        </div>
      </footer>
    <% end %>
    <% if @product_rsc_document_props || @public_rsc_props %>
      <%= stylesheet_pack_tag crossorigin: "anonymous" %>
      <%= javascript_pack_tag crossorigin: "anonymous", nonce: SecureHeaders.content_security_policy_script_nonce(request) %>
    <% elsif !@hide_layouts %>
      <%= vite_typescript_tag "base", skip_style_tags: true %>
    <% end %>
    <%= yield :script %>
  </body>
</html>
